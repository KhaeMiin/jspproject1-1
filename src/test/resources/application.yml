spring:
#  별도의 설정이 없으면 부트는 메모리모드로 돌려버린다. 그러므로 아래코드와 같이 메모리 모드 h2 setting이 별도로 필요하지 않음
#  datasource:
#    url: jdbc:h2:mem:test
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create
#    properties:
#      hibernate:
##        show_sql: true
#        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace